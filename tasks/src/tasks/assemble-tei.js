import { getStoreHandle, getLogger } from "/srv/api/src/common/index.js";
const log = getLogger();

export async function assembleTeiDocument({ directory }) {
    // code to assemble the final TEI document
    //   name the output file `{identifier}-tei-complete.xml`
}
